#include <bits/stdc++.h>
using namespace std;

typedef long long ll;


int main(){
    // assign the sign to the element ofn the array such that there sum will be equal to the 
    // given diffrerence
    // so it is basically equal to the problem of the (s1-s2=diff) click the question it is literally the same 
    //  as we have done before 
    return 0;
}