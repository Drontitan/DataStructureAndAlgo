#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

// divide the array such that two partition have equal sum

// note s can not be odd it cant be divided int to two equal parts 
int main(){
    // here we have to part the eqaul array in 2 subset such that there sum willbe equal to sum of the whole array
    // simple s/2;
    // the target will be s/2 then follow 14 video just that`s it 
    return 0;
}